# Климат-контроль Niva Travel — описание проекта

## 1. Назначение
Ретрофит-блок климат-контроля для LADA Niva Travel на базе STM32.
Управляет четырьмя моторедукторами заслонок (с позиционной обратной связью через
потенциометры), читает датчики и обеспечивает пользовательское управление панелью.

Первоисточник схемы — **проект KiCad** (`climate_control_niva_travel.kicad_sch` + страницы).
JSON-модель и скрипты — вспомогательные (генерация/проверки).

## 2. Архитектура (иерархия листов)
Корень — **CONTROLLER** (U1, развязка, SWD, reset, питание 3,3В), ниже 9 страниц:

| Стр. | Лист | Содержимое |
|---|---|---|
| 1 | CONTROLLER | STM32F103RCT6 (LQFP-64), C5–C10, FB1, SWD J_DBG, NRST/BOOT0 |
| 2 | CAN | трансивер MCP2562 (шина CANH/CANL → J1:25/26) |
| 3 | PWR | разъём J1 (32), защита D1/D2/D4/D5, L1/C20, стабилизаторы U6(5В), U7(3,3В), фильтры |
| 4 | UI | 6 кнопок, 6 светодиодов, ручки J_POT/J_SW, подсветка ILLUM |
| 5 | SEN_CABIN | NTC салона RT1 + вентилятор продувки |
| 6 | SEN_HEAT | NTC на патрубке радиатора печки |
| 7 | SEN_SOLAR | солнечная радиация (делитель 10k/20k) |
| 8 | SEN_COND | конденсат на стекле (делитель 10k/20k) |
| 9 | OUT | ШИМ вентилятора печки (J1:23), запрос A/C (J1:24) |
| 10 | ACT | 4× DRV8871 (H-мосты), R_ILIM, делители ОС заслонок |

## 3. Аппаратные решения
- **MCU**: STM32F103RCT6 LQFP-64 (0,5мм), CAN (PB8=RX61, PB9=TX62), HSI (PD0/PD1 свободны).
- **Моторы**: 4 моторедуктора заслонок, по 3 провода (A, B, ОС-потенциометр);
  потенциометры на общих +5В_REF/DGND (J1:17/18). Токоограничение — R_ILIM.
- **Разъём J1 (32п)** — распиновка в `circuit_model.json` (metadata.j1_pinout):
  1/2 +12В (дубль), 3/4 GND_PWR (дубль), 5–16 моторы A/B/FB, 17 +5В_REF,
  18 DGND, 19 NTC_HEAT, 20 SOLAR, 21 COND, 22 ILLUM, 23 FAN_PWM, 24 AC_REQ,
  25 CANH, 26 CANL, 27–32 резерв.
- **Защита питания**: переполюсовка D1 (SS54), TVS D2 (SMBJ15A), входной
  фильтр L1+C20, супрессор D5 (5,6В) на линии датчиков, D4 — развязка от обратного +12В.
- **Датчики**: NTC салона (на плате, в трубе продувки), NTC на патрубке печки,
  солнце и конденсат (0–5В через делители на АЦП 3,3В).

## 4. Генерация и скрипты
- `circuit_model.json` — модель (118 компонентов, 85 цепей): components, nets, sheets, metadata.
- `make_netlist_json.py` — сборка модели + канонизация ссылок (KiCad-формат `R1`, `C10`).
- `build_project.py` — строковый генератор `.kicad_sch` (содержимое страниц; листы-ссылки
  в этой сборке KiCad не эмитятся — их создаёт библиотека, см. ниже).
- `gen_schematic_api.py` — **основной генератор** на `kicad_sch_api` (установлен в `env`):
  строит все страницы + корень с листами-ссылками, пинами и метками на выводах U1.
- `gen_tests.py` / `kicad_to_spice.py` — генерация SPICE-тестов из нетлистов страниц.
- `run_tests.py` — запуск всех Ngspice-сценариев с авто **PASS/FAIL** (сейчас 18/18).
- `run_e2e.py` — **сквозной прогон через эмулятор** (оркестратор, без дублирования логики):
  (1) экспорт нетлистов `kicad-cli sch export netlist` → `spice/<SHEET>.net`;
  (2) генерация `.sub`/`.cir` через `kicad_to_spice.py` с портами из нетлиста;
  (3) прогон `run_tests.py` (Ngspice + PASS/FAIL). `python3 run_e2e.py` — полный цикл
  (флаги `--gen`, `--run`, `--sheet=<ЛИСТ>`), код возврата = число провалов.
  Описание — в `E2E_TEST_NGSPICE.md`.
- `mcp_automate.py` — вызов MCP-сервера напрямую (JSON-RPC) для создания листов/пинов.
- `wire_root.py` — построение связей на всех листах по `circuit_model.json`: от каждого
  вывода, входящего в цепь, рисуется провод-отвод + метка имени цепи (на корне — `label`,
  на дочерних листах для цепей с пином листа — `hierarchical_label`). Метка ставится по
  направлению вывода (углу пина): для правых выводов текст вправо от точки соединения,
  для левых — влево (`justify right`). Пины листов получают провод+метку, листы-ссылки
  привязываются к сетке 1.27 мм; для пинов, не входящих в цепи, ставятся `no_connect` +
  `PWR_FLAG` на силовые цепи. Идемпотентен: `python3 wire_root.py`, проверка — `--dry`.
  ERC (`kicad-cli sch erc`) — 0 нарушений.

## 5. SPICE-модели (в корне проекта)
- `drv8871_ngspice.lib` — поведенческий H-мост DRV8871;
- `mcp2562_ngspice.lib` — поведенческий CAN-трансивер MCP2562;
- `mc78m05_ngspice.lib` — стабилизатор 12→5В;
- `ams1117_33_ngspice.lib` — LDO 5→3,3В;
- Simulation_SPICE — диоды/транзисторы (системная библиотека KiCad).

## 6. Тесты (Ngspice)
`python3 run_e2e.py` — сквозной сценарий: экспорт нетлистов → генерация `.cir` →
прогон в эмуляторе → **PASS/FAIL** (вызывает `kicad-cli`, `kicad_to_spice.py`, `run_tests.py`).
Отдельные шаги: `run_tests.py` — только прогон готовых `.cir`; `gen_tests.py`/`kicad_to_spice.py` —
генерация. Сценарии — в `TEST_SCENARIOS_NGSPICE.md`, допуски — в `CHECKS` (`run_tests.py`).
Покрытие: PWR (5), UI (3: температура/галетник/подсветка), SEN_HEAT (2),
SEN_SOLAR/SEN_COND/SEN_CABIN, OUT (2), ACT (H-мост), CAN (рецессив/доминант) — **18/18 PASS**.

## 7. Ключевые файлы
```
climate_control_niva_travel.kicad_pro      проект KiCad
climate_control_niva_travel.kicad_sch     корень (CONTROLLER)
cc_0*.kicad_sch                            дочерние листы
circuit_model.json                         модель (компоненты/цепи/листы/распиновка)
make_netlist_json.py                       сборка модели
build_project.py / gen_schematic_api.py    генераторы схемы
kicad_to_spice.py / gen_tests.py           генерация SPICE
run_tests.py                               автотесты PASS/FAIL
run_e2e.py                                 сквозной прогон (экспорт+генерация+эмулятор)
E2E_TEST_NGSPICE.md                        описание сквозного сценария
TEST_SCENARIOS_NGSPICE.md                  перечень сценариев
drv8871_ngspice.lib, mcp2562_ngspice.lib,
mc78m05_ngspice.lib, ams1117_33_ngspice.lib  SPICE-модели
spice/                                     сгенерированные .sub/.cir
env/                                       виртуальное окружение (kicad-sch-api, mcp)
```

## 8. Ограничения / TODO
- Headless (CLI) не читает иерархию — проверка связей — через `validate_sheet_pins` (MCP),
  `wire_root.py --dry` и открытие в KiCad GUI. ERC из CLI (`kicad-cli sch erc`) — 0 нарушений.
- Кнопки/светодиоды/цифровая логика МК не моделируются в SPICE (MCU модели нет).
- CAN вынесен отдельным листом; CAN-сценарии есть.
- Резерв J1:27–32 свободен (будущие сигналы) — на схеме помечены `no_connect`.
- Старые дубли страниц (`cc_02_pwr`, `cc_03_ui`, `cc_04_sen_*` и т.п.) в `.history/`
  не используются иерархией (актуальные листы — `cc_0X_*.kicad_sch` из `sheets` модели).
```